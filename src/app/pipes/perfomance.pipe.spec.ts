import { PerfomancePipe } from './perfomance.pipe';

describe('PerfomancePipe', () => {
  it('create an instance', () => {
    const pipe = new PerfomancePipe();
    expect(pipe).toBeTruthy();
  });
});
